export default class NewObject {

    // constructor(props: any) {

    //     for (const key in props) {
    //         if (Object.prototype.hasOwnProperty.call(props, props)) {
    //             const ele = props[props];
    //             (this as any)[key] = ele;
    //         }
    //     }
    // }
    // setItem(path: string, value:) {

    // }
}